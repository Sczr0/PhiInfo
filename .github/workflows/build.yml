name: Multi-platform Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    name: Build - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            binary_name: PhiInfo.CLI.exe
          - os: ubuntu-latest
            rid: linux-x64
            binary_name: PhiInfo.CLI
          - os: macos-latest
            rid: osx-x64
            binary_name: PhiInfo.CLI

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0.x

    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: sudo apt-get update && sudo apt-get install -y clang zlib1g-dev

    - name: Download classdata.tpk
      run: |
        curl -L -o classdata.zip https://nightly.link/AssetRipper/Tpk/workflows/type_tree_tpk/master/lz4_file.zip
        unzip classdata.zip
        mv *.tpk classdata.tpk
      shell: bash

    - name: Publish
      run: dotnet publish src/PhiInfo.CLI/PhiInfo.CLI.csproj -c Release -r ${{ matrix.rid }} -o ./publish

    - name: Copy classdata.tpk to publish dir
      run: cp classdata.tpk ./publish/
      shell: bash

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: PhiInfo-${{ matrix.rid }}
        path: ./publish/
